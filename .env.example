# Copie para .env (que NÃO vai para o GitHub) e ajuste.
# Nenhum secret real deve ser commitado. Em produção, use as Environment Variables do Render.
ENV=development
LOG_LEVEL=INFO
PUBLIC_BASE_URL=http://localhost:8000
